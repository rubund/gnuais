#!/usr/bin/make -f

#export DH_VERBOSE=1

export DEB_LDFLAGS_MAINT_APPEND = -Wl,--as-needed

%:
	dh  $@

override_dh_strip:
	dh_strip -pgnuais --dbg-package=gnuais-dbg
	dh_strip -pgnuaisgui --dbg-package=gnuaisgui-dbg
